services:
  discoveryserver:
    build:
      context: .
      dockerfile: discoveryserver/Dockerfile
    ports:
      - "8761:8761"

  apigateway:
    build:
      context: .
      dockerfile: apigateway/Dockerfile
    ports:
      - "8088:8088"
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: https://discoveryservernew.internal.mangobush-8de88b36.southeastasia.azurecontainerapps.io:8761/eureka
    depends_on:
      - discoveryserver

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      - "3306:3306"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.3
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.5.3
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  mainservice:
    build:
      context: .
      dockerfile: mainservice/Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - kafka
      - discoveryserver
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: https://discoveryservernew.internal.mangobush-8de88b36.southeastasia.azurecontainerapps.io:8761/eureka
      JWT_SECRET: ${JWT_SECRET}

  iot-service:
    build:
      context: .
      dockerfile: IOT_service/Dockerfile
    ports:
      - "8082:8082"
    depends_on:
      - postgres
      - kafka
      - discoveryserver
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: https://discoveryservernew.internal.mangobush-8de88b36.southeastasia.azurecontainerapps.io:8761/eureka

  chatbotbackend:
    build:
      context: .
      dockerfile: chatbotbackend/Dockerfile
    ports:
      - "8081:8081"
    depends_on:
      - discoveryserver
    env_file:
      - .env
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: https://discoveryservernew.internal.mangobush-8de88b36.southeastasia.azurecontainerapps.io:8761/eureka
      GEMINI_API_KEY: ${GEMINI_API_KEY}

  vitalreports:
    build:
      context: .
      dockerfile: vitalReports-AI/Dockerfile
    ports:
      - "8083:8083"
    depends_on:
      - discoveryserver
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: https://discoveryservernew.internal.mangobush-8de88b36.southeastasia.azurecontainerapps.io:8761/eureka

  prometheus:
    image: prom/prometheus:v2.54.1
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      - discoveryserver
      - apigateway
      - mainservice
      - iot-service
      - chatbotbackend
      - vitalreports

  grafana:
    image: grafana/grafana:11.1.4
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
